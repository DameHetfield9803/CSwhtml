@model Poll;

<div class="container">

    <h2>
        @Model.Question
    </h2>

    <div class="row">
        <div class="col-sm-5">
            <h2 style="text-align:center">
                <a href="@ViewData["Url"]?choice=A">
                    @Model.ChoiceA
                </a>
            </h2>
            @{
            }
            <qrcode width="100" height="100">@ViewData["Url"]?choice=A</qrcode>

        </div>

        <div class="col-sm-2">
            @*Space between QR codes *@
        </div>

        <div class="col-sm-5">
            <h2 style="text-align:center">
                <a href="@ViewData["Url"]?choice=B">
                    @Model.ChoiceB
                </a>
            </h2>
            @{
            }
            <qrcode width="100" height="100">@ViewData["Url"]?choice=B</qrcode>
        </div>
    </div>
</div>
@*Damien Foo (cshtml)*@